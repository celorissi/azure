apiVersion: v1
kind: Pod
metadata:
  name: pod-4
  labels:
    name: pod-4
spec:
  containers:
  - name: pod-4
    image: nginx
    resources:
      limits:
        memory: "128Mi"
        cpu: "10m"
      requests:
        cpu: "10m"
    ports:
      - containerPort: 80