apiVersion: v1
kind: Pod
metadata:
  name: pod-3
  labels:
    name: pod-3
spec:
  containers:
  - name: pod-3
    image: nginx
    resources:
      limits:
        memory: "128Mi"
        cpu: "10m"
      requests:
        cpu: "10m"
    ports:
      - containerPort: 80


